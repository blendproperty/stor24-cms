'use client'
import React from 'react'

export const Icon = () => {
  return (
    <span
      style={{
        display: 'inline-flex',
        alignItems: 'center',
        justifyContent: 'center',
        width: '22px',
        height: '22px',
        borderRadius: '6px',
        background: '#ff5a0a',
        color: '#f5f3ea',
        fontWeight: 800,
        fontSize: '12px',
      }}
    >
      S
    </span>
  )
}

export default Icon
