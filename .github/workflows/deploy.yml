name: Deploy to VPS

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to VPS
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          port: 22
          timeout: 60s
          command_timeout: 10m
          script: |
            cd /opt/stor24-cms
            git pull origin main
            docker compose up -d --build
            echo "Waiting for container to be ready before migrating..."
            sleep 10
            docker compose exec -T stor24-cms npx payload migrate
            echo "CMS deployed and migrated successfully"
